//this file will read  our mockData file,
//and write it to db.json file which will be read by apiServer
/* eslint-disable no-console */
const fs = require("fs");
const path = require("path");
const mockData = require("./mockData");
